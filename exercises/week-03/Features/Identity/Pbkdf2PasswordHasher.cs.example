// Target path: src/EcoBazaar.Api/Features/Identity/Pbkdf2PasswordHasher.cs
// Purpose: hash and verify passwords without storing plain text.

namespace EcoBazaar.Api.Features.Identity;

public sealed class Pbkdf2PasswordHasher
{
    // Generate a random salt.
    // Derive a hash with PBKDF2-SHA256.
    // Store algorithm, iteration count, salt, and hash.
    // Verify with fixed-time comparison.
}
